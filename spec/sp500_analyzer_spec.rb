require "spec_helper"

describe SP500Analyzer do
  it "has a version number" do
    expect(SP500Analyzer::VERSION).not_to be nil
  end

  # it "does something useful" do
  #   expect(false).to eq(true)
  # end
end
