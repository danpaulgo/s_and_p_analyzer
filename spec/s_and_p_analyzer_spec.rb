require "spec_helper"

describe SAndPAnalyzer do
  it "has a version number" do
    expect(SAndPAnalyzer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
